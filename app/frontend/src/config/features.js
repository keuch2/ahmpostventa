/*
 * Flags de funcionalidades del frontend público.
 *
 * AUTH_BUTTONS_VISIBLE: muestra u oculta los botones "Iniciar sesión" (header)
 * y "Iniciar sesión" / "Registrarse" (hero de la home).
 * PENDIENTE: volver a poner en true cuando se habilite el acceso de clientes.
 */
export const AUTH_BUTTONS_VISIBLE = false;
